---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [client/, server/, package.json, .gitignore]
autonomous: true
requirements: [RESM-01, AIAN-01]
---

# Plan: 01-01 - MERN Foundation Initialization

Initialize the project structure with separate `client` and `server` directories, setting up React with Vite and a basic Express server.

<objective>
Establish a working MERN project structure with a React frontend and Node.js backend.
</objective>

<tasks>
<task type="execute">
<files>
- .gitignore
- package.json
</files>
<read_first>
- .planning/research/STACK.md
</read_first>
<action>
Create a root `.gitignore` file and a basic `package.json` for task management.
Add `node_modules`, `.env`, `dist`, and `.planning/` (optional based on config) to gitignore.
</action>
<verify>
`ls -a` shows .gitignore.
</verify>
<acceptance_criteria>
- [ ] Root `.gitignore` exists and contains `node_modules` and `.env`.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- client/
</files>
<read_first>
- .planning/phases/01-foundation/01-CONTEXT.md
</read_first>
<action>
Initialize React frontend using Vite in the `client` directory.
Run `npm create vite@latest client -- --template react`.
Install dependencies: `tailwindcss postcss autoprefixer lucide-react axios framer-motion`.
Initialize tailwind: `npx tailwindcss init -p`.
</action>
<verify>
`ls client/package.json` exists.
`client/src/main.jsx` exists.
</verify>
<acceptance_criteria>
- [ ] `client` directory exists with a Vite/React structure.
- [ ] `client/package.json` contains `framer-motion` and `tailwindcss`.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- server/
</files>
<read_first>
- .planning/phases/01-foundation/01-CONTEXT.md
</read_first>
<action>
Initialize Node.js backend in the `server` directory.
Run `npm init -y` inside `server`.
Install dependencies: `express mongoose dotenv cors jsonwebtoken bcryptjs pdf-parse @google/generative-ai`.
Create a basic `index.js` that starts an Express server on port 5000.
</action>
<verify>
`ls server/package.json` exists.
`server/index.js` exists and contains `express`.
</verify>
<acceptance_criteria>
- [ ] `server` directory exists with `package.json` and `index.js`.
- [ ] `server/package.json` contains `express` and `@google/generative-ai`.
</acceptance_criteria>
</task>
</tasks>

<verification>
1. Run `npm install` in both `client` and `server`.
2. Start the server with `node index.js` and verify it listens on port 5000.
</verification>

<success_criteria>
- [ ] Project structure has `client/` and `server/` directories.
- [ ] React app is initialized and has Tailwind CSS dependencies.
- [ ] Express server is initialized with Gemini and MongoDB dependencies.
</success_criteria>

---
## PLANNING COMPLETE
