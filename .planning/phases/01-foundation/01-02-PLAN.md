---
phase: 01
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [server/.env, server/config/db.js, server/index.js]
autonomous: true
requirements: [RESM-01, AIAN-01]
---

# Plan: 01-02 - External Service Configuration

Setup environment variables and establish connections to MongoDB Atlas and the Google Gemini API.

<objective>
Verify that the backend can successfully connect to the database and call the AI model.
</objective>

<tasks>
<task type="execute">
<files>
- server/.env
- server/.env.example
</files>
<read_first>
- server/package.json
</read_first>
<action>
Create `.env` and `.env.example` in the `server` directory.
Include variables: `PORT=5000`, `MONGO_URI`, `GEMINI_API_KEY`.
</action>
<verify>
`ls server/.env` exists.
</verify>
<acceptance_criteria>
- [ ] `server/.env` exists with necessary keys.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- server/config/db.js
</files>
<read_first>
- server/index.js
</read_first>
<action>
Create a database configuration file using Mongoose.
Implement a `connectDB` function that logs success or failure.
</action>
<verify>
`server/config/db.js` exists and contains `mongoose.connect`.
</verify>
<acceptance_criteria>
- [ ] Database connection logic is modularized in `db.js`.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- server/index.js
</files>
<read_first>
- server/config/db.js
</read_first>
<action>
Update `server/index.js` to:
1. Load environment variables.
2. Call `connectDB()`.
3. Add a `/api/test-gemini` GET endpoint that calls the Gemini API with a simple prompt and returns the response.
</action>
<verify>
`server/index.js` imports `connectDB` and has the test endpoint.
</verify>
<acceptance_criteria>
- [ ] Server successfully initializes database connection on startup.
- [ ] `/api/test-gemini` endpoint exists and returns a JSON response.
</acceptance_criteria>
</task>
</tasks>

<verification>
1. Run `npm start` in `server`.
2. Check logs for "MongoDB Connected".
3. Use `curl http://localhost:5000/api/test-gemini` (if API key provided) to verify AI connectivity.
</verification>

<success_criteria>
- [ ] Environment variables are configured.
- [ ] MongoDB connection is functional.
- [ ] Gemini API integration is verified with a test endpoint.
</success_criteria>

---
## PLANNING COMPLETE
